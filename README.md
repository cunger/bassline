# Bassline

_Bassline_ is a Sinatra template that provides a basic project structure with files to get me started. No database, no fancy stuff.

## Usage

* Run `bundle install`
* Run tests with `rake test`
* Start with `rackup`
